const add = (a, b) => {
    return a + b
}

const sub = (a, b) => {
    return a - b
}

const mul = (a, b) => {
    return a * b
}

const name = 'shubham'

// module.exports.add = add
// module.exports.sub = sub
// module.exports.name = name

module.exports = { add, sub, name, mul };
